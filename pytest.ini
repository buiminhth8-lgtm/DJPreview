[pytest]
markers =
    slow: 慢速集成测试（API / 渲染 / 全链路），默认被快速检查跳过
filterwarnings =
    # TrackSpec.register 是协议要求的字段名，与 BaseModel 属性重名仅为无害警告
    ignore:Field name "register" in "TrackSpec" shadows an attribute in parent "BaseModel":UserWarning
